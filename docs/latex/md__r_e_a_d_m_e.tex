Set of classes written on C++ useful for creating simple parsers.

As of the latest commit, the classes included are ready to be used. Some minor changes might happen, but they can be used to create simple tokenizers and parsers.

An example of how to use some of the classes to obtain tokens from a line\-:

``` \#include \char`\"{}\-My\-Automata.\-h\char`\"{} \#include \char`\"{}\-My\-Keyword\-Set.\-h\char`\"{} \#include \char`\"{}./\-Core/\-Token.\-h\char`\"{} \#include \char`\"{}./\-Tokenizer/\-Tokenizer.\-h\char`\"{}

\#include $<$iostream$>$ \#include $<$string$>$

using namespace std;

int main() \{ /// \doxyref{Abstract\-Automata}{p.}{class_abstract_automata} must be subclassed. /// \doxyref{Keyword\-Set}{p.}{class_keyword_set} might be subclassed or not. \doxyref{Tokenizer}{p.}{class_tokenizer} tokenizer(new My\-Automata, new My\-Keyword\-Set); \doxyref{Token}{p.}{class_token} $\ast$ token = 0; string line = \char`\"{}\-Some line here...\char`\"{};

/// The transitions matrix must be set. Method returns false if /// something fails. tokenizer.\-automata().load\-Matrix(\char`\"{}path\-\_\-to\-\_\-file/transtions\-\_\-file.\-txt\char`\"{}); /// line could come from a different source (a file, for example) tokenizer.\-set\-Line(line); token = tokenizer.\-get\-Token(); while (token != 0) \{ /// Do something with token here. delete token; token = tokenizer.\-get\-Token(); \}

return 0; \} ```

More examples and documentation will be added later. 